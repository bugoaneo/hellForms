@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Web.Common.PublishedModels.HeaderSection>
<section class="block block--form" style=" --veil:0%;">
    <div class="block__content">
        <div class="block__header">
            <h2 class="block__title">
                @Html.Raw(Model.Title)
               @*  @if (Model.HasValue("titleHint"))
                {                    
                    <i class="hintIcon">?</i>
                    <span class="hintBody text">
                        @Model.Value("titleHint")
                    </span>
                } *@
            </h2>
        </div>
        @if (Model.HasValue("description"))
        {
            <div class="block__description text">
                @Model.Description
            </div>
        }
    </div>
</section>